// video no.-5/51 chai aur code js playlist 51 videos
/*before 8-10 yrs ago js is bit different from today js classes, module, arrow function these are not before.
And many codes are already written in older version
And js committey in their organisation don't want to change it.
And when we are using new codes then their should be some standards, 
we are using all codes in new version and all codes whether is new or old will be going to use in same machine. 
so for this in js we write "use strict" because with the help of this all codes can be treated in newer version also. 
*/

"use strict"; // treat all js code as newer js/version ans once we used this command then all codes will be used in newer version
// by the way today it becomes more standard so all codes were used in newer version in strict mode, so we don't need to write this "use strict"

//alert("hello")

//alert(3 + 3) // we are using node.js, not browser

console.log(3+3); 

console.log("Tannu") // don't write these types of code

// codes readability should be high represented in good way

let name = "Tannu"
let age = 20
let isLoggedIn = false

// number => 2 to the power 53 (it is basicaaly range)
// bigint used if number is very big but generally in programming is not as that big, so that we have to use this
// string => "" or ''
// boolean => true/false
// null => in js it is standalone a value and important, it represntation of empty value, and we assign to variable ex -> let state = null
//undefined => when value is not defined to variable as we have seen earier. ex-> let state;
// symbol => unique

//object

console.log(typeof "Tannu");
console.log(typeof undefined); //undefined because it is 
console.log(typeof null); //object


